// Imports

// Actions

// Action Creators

// API Actions

// Initial state

const initialState = {
    isLoggedIn: false
}

// Reducer

function reducer(state = initialState, action){
    switch (action.type){
        default:
            return state;
    }
}

// Export

// Default Reducer Export

export default reducer